---
  title: Intento
  description: The Intento Blackbird app
---
import { LinkCard } from "@astrojs/starlight/components";
import { YouTube } from '@astro-community/astro-embed-youtube';

<LinkCard title="View on Github" target="_blank" href="https://github.com/bb-io/Intento" icon="github" />

The Intento app enables Blackbird workflows to use Intento for machine translation, translation quality review, OCR, text intelligence, and usage analytics.

### Translation

- **Translate text**: Translates text from one language to another.
- **Translate**: Translates files using one of two strategies: `blackbird` for interoperable translation workflows and `intento` for native document translation.
  Native Intento strategy supports:
  `TXT`, `HTML`, `HTM`, `XML`, `SRT`, `ICU`, `PDF`, `DOCX`, `XLSX`, `PPTX`, `PNG`, `JPG`, `JPEG`, `BMP`, and `ZIP`.
  When a file format is not supported by the native Intento strategy, the app automatically falls back to the `blackbird` strategy.
  For XLIFF/XLF files, the app uses native Intento only when the file can be mapped to `xliff-2.0`; otherwise it falls back to `blackbird`.

### Review

- **Review text**: Review translation quality for source and target text using IntentoQA.
- **Review**: Review translation quality using IntentoLQA for bilingual files, marks segments above the threshold as final, and returns the reviewed file together with summary metrics.
- **Review in background with Intento LQA**: Submits a bilingual file for review with IntentoLQA, returns a job ID, and allows users to check the status of the review job and retrieve results when the job is complete.
- **Download background review results** : Downloads the reviewed file and summary metrics for a completed background review job.

### Event

- **On background review with Intento LQA finished**: Triggered when all provided Intento LQA search keys have evaluations.

### Usage

- **Get usage statistics**: Retrieves aggregated usage statistics from Intento for either Intento usage or provider usage endpoints.

### Glossaries

- **Search glossaries**: Returns the Intento glossaries available to the connected account.
- **Create or update glossary**: Creates a bilingual glossary or merges terms into an existing bilingual glossary from a Blackbird interoperable TBX file. Source and target languages are required; for multilingual files, only terms containing the selected language pair are imported. This action creates draft changes and does not publish them automatically.
- **Download glossary**: Downloads published or draft Intento glossary terms as a Blackbird interoperable TBX file.

The current create or update behavior is an upsert: terms present in the input TBX are added or updated, while terms omitted from the file are not deleted.
Because Intento terms do not contain locale metadata, each Intento glossary managed by this action must contain exactly one language pair. Run the action separately for every target language.

## Feedback

Do you want to use this app or do you have feedback on our implementation? Reach out to us using the [established channels](https://www.blackbird.io/) or create an issue.

